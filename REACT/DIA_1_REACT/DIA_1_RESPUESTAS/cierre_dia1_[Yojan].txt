1. En el Sprint 0 tenías renderMenu() con innerHTML. Hoy construiste PlatoCard.
¿Qué diferencia concreta hay en cómo cada uno actualiza la pantalla cuando cambia un solo plato? Menciona el Virtual DOM en tu respuesta.

- En este caso la diferencia notoria es que con el innethtml se borraba todo el contenido del DOM, por lo cual se creaba desde cero. Pero con React se usa el virtual DOM, este compara el estado anterior con el nuevo y así solo actualiza el elemento editado a comparación del primero.

2. ¿Qué es JSX exactamente? ¿Es HTML? ¿Es JavaScript? ¿Qué hace Vite con él antes de que llegue al navegador?

- Es como que medio combiana ambas cosas, por decirlo parece HTML pero en realidad en JavaScript. En cuanto al Vite este se encarga de procesar el JSC, convertirlo en un JavaScript válido y optimizarlo 

3. PlatoCard recibe los mismos campos que va a devolver GET /api/platos del equipo de Node.
¿Por qué eso importa? ¿Qué van a tener que cambiar en PlatoCard cuando el endpoint esté listo?

- Es importante porque  asi el componenete etsá la listo para trabajar con datos reales y en cuanto a que vamos a tener que cambiar cuando edpoint esté listo, solo sería la fuente de datos. 

4. Mañana instalan Vite y crean el proyecto real.
¿Qué problema resuelve Vite que hoy resolvieron con el CDN de React?

- Pues sería tener un mejor orden de las cosas, así aumentando el rendimiento y recargar automáticamente al guardar cambios


