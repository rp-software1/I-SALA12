¿Por qué React obliga a usar setCarrito en lugar de mutar el array directamente con push? ¿Qué mecanismo interno de React hace que esto sea necesario?

Por que React funciona detectando cambios de las referencias de los estados como cuando use el setCarrito. El mecanismo que lo hace necesario es por medio de la reconciliacion para que asi compare los estados y props.

Hoy usaron useEffect con [] para cargar el menú. Mañana van a usar Axios para hacer un GET real a /api/platos. ¿Qué parte del código de hoy va a cambiar y qué parte va a quedarse igual?

Se podria decir que cambiara la parte de obtener datos ya que se podria decir que enves de usar platosmock, usaremos un http y en la parte del estado quedaria igual por motivos de que necesitariamos donde guardar los datos.

En el Sprint 0 calculaban el total del carrito con un for loop. Hoy usaron reduce. ¿Son equivalentes? ¿En qué situación usarías uno sobre el otro?

Se diria que son equivalestes ya que los 2 calculan el total pero de diferente forma. El reduce, se usaria mas  cuando es un calculo corto , sumar totales, calcular promedios y for seria cuando la logica es compleja o si uso o necesito muchos if. 

Si mañana necesitaran compartir el carrito entre dos componentes distintos — por ejemplo, CarritoPage y una barra de navegación que muestra cuántos ítems hay — ¿dónde tendría que vivir el estado del carrito? ¿Por qué?

se necesitaria un componente padre que pueda funcionar a ambos, por que CarritoPage tiene un flujo de datos de padre a hijo.