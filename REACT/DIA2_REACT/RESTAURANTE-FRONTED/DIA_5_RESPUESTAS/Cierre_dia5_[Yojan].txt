¿Por qué React obliga a usar setCarrito en lugar de mutar el array directamente con push?
Es porque necesita este necesita detectar cuando cambia el estado para volver a renderizar la interfaz
¿Qué mecanismo interno de React hace que esto sea necesario?
Pasa que comprara referencias en memoria para detectar cambios de estado por eso se llega a crear un nuevo array y React detecta esta nueva referencia.

Hoy usaron useEffect con [] para cargar el menú. 
Mañana van a usar Axios para hacer un GET real a /api/platos.
¿Qué parte del código de hoy va a cambiar y qué parte va a quedarse igual?
La parte que cambiaría se´ria la obtención de datos para taer los platos al backend y lo que se quedaría igual sería el useEffect, el array [], los estados y el renderizado condicional.


En el Sprint 0 calculaban el total del carrito con un for loop. Hoy usaron reduce. 
¿Son equivalentes? 
Si ya que ambos pueden calcular el total y llegar al mismo resultado
¿En qué situación usarías uno sobre el otro?
Usarpia reduce cuuando quiero transformar un array en un único valor como el total del carrito siendo de manera mas cota y limpia. Pero en cambio for loo cuando ocuparía hacer varias operaciones más complejas y así tener un mejor control paso a paso.

Si mañana necesitaran compartir el carrito entre dos componentes distintos 
— por ejemplo, CarritoPage y una barra de navegación que muestra cuántos ítems hay 
— ¿dónde tendría que vivir el estado del carrito?
Tendría que vivir en un componente padre compartido o más adelante en un Context.
¿Por qué?
Pasa que así los dos componentes usarían la misma fuente de datos y estaría sincronizados, caso contrario puede mostrar información diferente.



